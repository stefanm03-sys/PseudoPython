{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run PseudoPy (current workspace)",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/main.py",
      "args": ["pseudo.ppy"],
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
